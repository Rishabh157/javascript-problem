//  return()  => the reverse method reverse the elements positions in the array and it also modifies the original array 

const countries = ["India", "America", "Poland", "Norway", "Scotland", "Argentina", "Brazil", "Mexico", "Caneda"]

countries.reverse()
console.log(countries)
