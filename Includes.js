//  includes() =>  includes methods return true if presence of element are finded in the array. otherwise it return false 

const colorsArr = ["red", "blue", "green", "yellow"]

const isAvailableColor = colorsArr.includes("yellow")
console.log(isAvailableColor)
